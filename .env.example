# Base URL of the cms-content-engine API, without a trailing slash.
#
# Vite inlines VITE_-prefixed values at build time, so changing this on a
# hosting platform requires a redeploy rather than a restart.
VITE_API_BASE_URL=https://cms-content-engine.up.railway.app
